import "./Header.css";

const Header = () => {
  return (
    <header className="header">
      <nav className="nav">
        <a href="#">Blog</a>
        <a href="#">Press</a>
        <a href="#">Join Us</a>
      </nav>
    </header>
  );
};

export default Header;
